## Назначение задания №15 и проверка RLS (2026-09-25)

- После PR #97 миграция `preserve_legacy_task_finance_on_assignment` применена в Supabase, код слит и production deploy завершился успешно. Назначение инженера на импортированном задании сохраняет финансовые поля, триггер защиты остаётся активным.
- Пользователь назначил задание №15. Read-only проверка: назначенный профиль активен и имеет роль engineer; задание содержит одну импортированную финансовую строку, она не изменилась. Статус пока `draft`, даты не заданы. Статус и даты не менялись автоматически.
- Production RLS smoke в транзакции `READ ONLY` под ролью `authenticated` и JWT claims назначенного инженера вернул задание №15 и его строку. Политика `order_access` даёт чтение назначенным инженерам. UI-вход под инженером подтверждён; график оставался на «Работаю…», поэтому открытие задания в браузере не подтверждено.
- Production counts: 40 service_order_items; 17 зеркал legacy work и 23 зеркала legacy material; 0 request-finance generation 1, 0 аннулирований, 0 строк с фактом и 0 переносов.
- Локальная перепроверка: `npm test` 424/424, `npm run test:tz` 424/424 (`America/Los_Angeles`), `npm run lint`, `npm run build` и `node tests/smoke.cjs dist/index.html` успешно. PR #97 CI build + production Pages deploy и docs checkpoint run #391 завершились успешно.
- Следующие действия: выяснить причину зависающего экрана инженера и подтвердить открытие №15 в UI; провести полную online/offline replay + fact/carry проверку на non-production данных; не отключать legacy RPC/readers до завершения проверки длительно офлайн-клиентов. Supabase branch для тестов пока нет. Не добавлять факты или переносы в production.

# Engineer-role fix deployed (2026-09-24 17:30 UTC)

- В live smoke под `engineer` обнаружено, что открытие задания без команды вызывает предупреждение: task loader делает запрос к manager-only `settings.tariff_profiles`. Исправлено в PR #95, squash `5898096d157845427062685e012f5435e3f853ff`; production Pages обновлён, bundle `assets/index-BNK_5308.js`, предупреждение исчезло.
- Regression test проверяет, что engineer task view читает work catalog, но не обращается к приватным tariff settings. Lint, оба полных набора 422/422, build/preview smoke и `git diff --check` прошли.
- Аккаунт engineer видит 0 назначенных заданий; старые задания в выборке — draft без команды, факты и перенос отключены. Никаких assignment/task data в production не меняли.
- Через приложение назначение тестового задания было заблокировано до переноса экономики. Не обходить guard прямым UPDATE, пока не определены точный объект и требуемый finance transfer. Следующий шаг — read-only разбор соответствующей DB-функции/ошибки, затем non-production сценарий с назначением, material/work, partial fact и carry.

# Engineer UI smoke (2026-09-24 17:23 UTC)

- Вход под отдельной ролью `engineer` подтверждён видимым профилем приложения. Карточка задания №22 открывается по сохранённому URL, но задание черновое, команда/ответственный не назначены; поля управления и фиксации факта отключены.
- В диспетчерской выборке 16 черновых заданий без команды. Фильтр по вошедшему инженеру вернул 0 заданий; в колонках workflow также 0 записей. Поэтому живой тест записи факта/переноса и offline replay на назначенном задании пока не выполнен. Производственные записи не менялись.
- UI показывает предупреждение каталога работ: `Cannot coerce the result to a single JSON object`. В консоли ошибок с домена сайта не обнаружено (только нерелевантные ошибки расширения браузера). Причину предупреждения нужно отдельно проверить, если оно воспроизводится на назначенном задании.
- Следующий шаг для полного UI smoke: предоставить в non-production среде тестовое задание, назначенное этой инженерской учётной записи, с работой и материалом; добавить сценарий partial fact/transfer, если его ещё нет. Не назначать тестовые записи и не добавлять факты в production.

# Проверка после PR #94 (2026-09-24 17:00 UTC)

- После production rollout PR #93 добавлен и смержен отдельный regression test: когда в заявке присутствуют исходная финансовая строка и перенесённый остаток, остаток не добавляется второй раз в request finance projection. PR #94: https://github.com/DlightKND/route-planner/pull/94, squash commit `ea0293b2966fcd04a5b51f8c0075209025dfcfcf`.
- Повторная проверка текущей реализации: lint, полный Vitest-набор 421/421, timezone-набор 421/421 (`America/Los_Angeles`), production build/preview smoke и `git diff --check` прошли.
- Ранее выполненная read-only production-сверка остаётся без изменений: 17/17 legacy work и 23/23 material зеркал совпадают; в production пока нет строк фактического переноса/частичного выполнения для дополнительной сверки.
- Остаётся проверить полный UI/RLS/replay-сценарий под engineer-ролью, когда доступен отдельный инженерный вход. Не менять production-данные тестовыми строками.

# Передача работы: сервисный диспетчер

## Аннулирование финансовых строк: production rollout (2026-09-24 15:50 UTC)

- Добавлено менеджерское аннулирование финансовых строк базового задания с обязательной причиной и неизменяемым снимком исходных сумм, автора и исходной строки. Аннулированные строки исключены из проекций заявки и расчётов; в задании они остаются видимыми и заблокированными.
- Устаревшие снимки из старого RPC, нового редактора и прямое удаление legacy-строк больше не восстанавливают/не удаляют аннулированные суммы. Исправленную строку можно привязать к записи аннулирования; сами журнал и связь защищены RLS и клиентскими read-only grants.
- Production migrations применены: `audited_request_finance_void` (`20260924135338`), `index_request_finance_audit` (`20260924135441`), `lock_voided_request_approvals` (`20260924135617`). Production smoke проверил: журнал пуст, RLS включён, authenticated может вызывать manager RPC, anon не может, прямые INSERT в оба журнала запрещены. Новых security-advisor замечаний по добавленным объектам нет; performance advisor больше не показывает неиндексированные FK, только неиспользованные пока индексы пустых журналов.
- Повторная локальная проверка после rollout: lint, build/preview/smoke, `git diff --check`, обычный и timezone полный набор по 420/420 тестов; DB migration suite — 28/28.
- PR #93 `[deploy] Add audited finance void and correction flow` слит squash-коммитом `af2096ce3ef33c4c04ec3c24ee8239548004beac`. PR build/CI run #375 и production Pages deploy run #376 завершились успешно.
- Живой UI проверен под admin-ролью: открыт мигрированный task и связанная заявка. Исторические финансовые поля заблокированы, отдельный «Аннулировать» открывает диалог с обязательной причиной; диалог отменён без сохранения. Ошибок сайта в консоли нет. Engineer-роль и полный save/replay-сценарий в UI ещё не проверены.
- Production economics read-only reconciliation: 17/17 legacy work и 23/23 material строки имеют ровно по одному зеркалу, совпадающие количество/суммы/подтверждение/исходный снимок; недостающих и лишних зеркал — 0. Все 40 строк имеют финансовые снимки. Тестовых изменений нет.
- В production пока 0 строк новой request-finance generation, 0 аннулирований, 0 переносов и 0 строк с фактом выполнения. Перенос остатка после частичного выполнения проверен DB regression test и сохраняет пропорциональные work/material economics; live примеров для сверки пока нет.

### Следующие шаги

1. Проверить engineer UI/RLS и полный online/offline replay на тестовых сеансах; manager UI read-only smoke выполнен, production-записи не менялись.
2. Когда появятся canonical finance и частично выполненные/перенесённые задания, сверить их production economics без суммирования исходной и перенесённой выручки.
3. Дождаться проверки старых клиентов перед отключением legacy RPC/readers; shadow-связи оставить нетронутыми до завершения этой проверки.

## Аудит исторических shadow-заданий и совместимости (2026-09-24)

- Read-only production-аудит всех семи строк service_orders с legacy_trip_id: все семь всё ещё ссылаются на существующий неудалённый выезд, обратная ссылка trips.service_order_id совпадает, по одной shadow-записи на выезд. У всех статус draft; service_order_items пуст.
- Старые service_order_jobs сохранены: пять выездов связаны с одной заявкой, ещё два — с двумя (всего девять связок). Каждая связка имеет соответствующее seed-задание seed_request_id=job_id и сохранённую связь trip_service_orders с link_source='historical_backfill'. Это подтверждает, что исходный выезд и новые задания уже связаны раздельно; shadow-записи не объединять и не удалять.
- В новом канбане сервисных заданий записи без job_id отфильтрованы; выезды находят актуальные задания через trip_service_orders. Shadow-строки не содержат service_order_items, поэтому текущий аудит не нашёл в них повторно перенесённых работ/материалов. Перед возможной архивацией нужно отдельно проверить старые клиенты и потребителей истории.
- Начальная инвентаризация очереди: новый редактор пишет job_request_save_canonical и ставит canonical_generation=1; повтор старой очереди без метки намеренно идёт через job_request_save. Устаревшую RPC пока нельзя отключать без проверки длительно офлайн-клиентов. Старые прямые job_parts операции остаются для совместимого fallback, canonical строки работают через RPC.
- Никаких production-строк не меняли. Следующий полезный этап: проверить видимость/историю этих shadow-записей на живой роли и сессии старого клиента; параллельно завершить manager/engineer UI smoke по коррекции, подтверждениям и replay.

## Проверка после canonical cutover и защита подтверждений (2026-09-24 13:10 UTC)

- Ревью последнего этапа воспроизвело дефект: неизменённый полный снимок заявки от инженера снимал подтверждение менеджера с новых canonical work/material строк. Проверка изменения `billable` у подтверждённого материала сравнивала прежнее значение с самим собой. Дополнительно обычные строки плана базового задания попадали в редактор заявки и могли попасть под удаление полным снимком; запрос с UUID материала в списке работ не проверял тип строки.
- PR #92 `[deploy] Preserve request finance approval and task ownership` слит в `48a13a4a5ee4b9cd17e44ad6e089fd360ac09c3e`. Новая миграция `20260924130146_preserve_request_finance_approvals.sql` применена к production; фактическая версия ledger `20260924131009`. `CREATE OR REPLACE` меняет только приватную `dlight_private.request_finance_save`; публичный RPC, старый offline replay, grants и таблицы сохранены.
- Теперь неизменённая подтверждённая строка сохраняет автора/время одобрения; изменение подтверждённого материала инженером отклоняется. Подтверждённые строки нельзя удалить обычным полным снимком. При replay устаревшего снимка отсутствие подтверждённой или чужой строки у инженера не удаляет её; новая строка хранит автора. RPC ограничен строками с provenance `request_finance_generation=1`, а редактор заявки не проецирует обычные task-plan items. UI скрывает удаление подтверждённых строк до отдельной аудированной коррекции.
- Production до миграции: 0 строк нового поколения, поэтому чинить уже сохранённые подтверждения не потребовалось. После миграции проверены определение функции и grants (`anon` не вызывает RPC). Транзакционный smoke создал строку под менеджером, неизменённо сохранил под назначенным инженером, проверил сохранение одобрения и откатил транзакцию; проверочных строк после rollback — 0. Первый smoke с неполным тарифным профилем закономерно отклонён; повтор использовал нормализованный сервером профиль и прошёл.
- Проверки: lint, `npm test` 416/416, `npm run test:tz` 416/416, build/smoke и diff check; PR-CI run #372 и Pages workflow #373 успешны. Живой сайт отдаёт `assets/index-ClW7g6-V.js` с `job_request_save_canonical`, фильтром `request_finance_generation` и защитной пометкой подтверждённой строки.

### Что остаётся

1. Реализовать отдельное аудируемое аннулирование/коррекцию финансовых строк с автором, причиной, прежними суммами и ссылкой на исправление; обычные DELETE и правка импортированных строк остаются закрытыми.
2. Проверить на реальном UI и в RLS путь менеджера и инженера для новой заявки, материалов, подтверждения, смены статуса и replay старых/новых офлайн-снимков. Для production пока нет постоянных новых canonical строк, поэтому транзакционный smoke не покрывает весь браузерный сценарий.
3. Провести отдельный аудит семи исторических shadow-заданий с `legacy_trip_id`, определить безопасную судьбу ссылок и сохранить историю; не объединять и не удалять автоматически.
4. Инвентаризировать все оставшиеся legacy write/read пути и очередь, затем выводить совместимость только после доказанного отсутствия старых клиентов. Отдельно сверить экономику task-only строк, переносов и частичного факта, чтобы не дублировать выручку.


## Контрольная точка: атомарное сохранение материалов заявки завершено (2026-09-24 11:24 UTC)

- PR #88 `[deploy] Save request materials atomically` слит squash-коммитом `598e92a582d564c79fc4bbd49c7153fd73268820`. PR-CI run #359 и production build/deploy run #360 успешны.
- Production migration `20260924112132 request_parts_write_rpc` применена к `anqfbljgfimoaziztdxe`. `public.job_request_save(uuid,jsonb,jsonb,jsonb)` — `SECURITY INVOKER`, `search_path=''`; `EXECUTE` разрешён `authenticated`, отозван у `anon` и `public`. Четвёртый параметр `p_parts` необязательный и имеет `null` по умолчанию, поэтому старые трёхпараметрические клиенты и queued payloads сохраняют совместимость.
- Шапка, работы, материалы и финальная смена статуса сохраняются в одной транзакции; материалы пишутся до смены статуса, чтобы сохранить действующую RLS-проверку инженера. Новые material UUID назначаются до офлайн-очереди для идемпотентного replay. Старые поштучные события `part` поддерживаются. Существующие триггеры синхронизации и защита удалений связанных материалов не отключались.
- Production transactional smoke под менеджерской ролью создал заявку и материал в явной транзакции, RPC вернул материал с `price=123` и `cost=45`; транзакция откатилась. Прямой SELECT строки в том же тестовом session context не показывал запись из-за RLS, поэтому подтверждение значений сделано по ответу RPC. Схема, grants и ledger проверены после применения.
- Pages отдаёт `assets/index-CbS-oxLz.js`; в опубликованном bundle подтверждены `p_parts`, `job_request_save` и `client_new`. Вход на сайт проверен, видна рабочая панель.
- Проверки: `npm run lint`, `npm test` — 408/408, `npm run test:tz` — 408/408, `npm run smoke` (build и preview smoke), `git diff --check`; PR-CI повторил lint, оба тестовых прогона, build и smoke успешно.
- Следующий этап: перевести редакторы на canonical writes с поддержкой старой офлайн-очереди; затем отдельно реализовать audited void/correction и провести аудит семи shadow-задач. Не отключать legacy paths и не менять/удалять shadow-задачи до соответствующих этапов. Shared-чат `https://chatgpt.com/share/6ab4fe25-15d0-83eb-bda2-b52a8e6abe2f` в этой среде не отдал содержимое, поэтому исходный ориентир — checkpoint репозитория.

## Контрольная точка: доступ инженера к базовому заданию заявки (2026-09-24 11:44 UTC)

- PR #89 `Allow request engineers to read seed tasks` слит (`13c7a0f871df83402221b5181df75ef13cc64c92`), PR-CI run #362 зелёный. Это изменение БД; Pages bundle не менялся.
- Production migration `20260924114345 seed_task_request_access` применена. `dlight_private.order_access` теперь даёт инженеру чтение только базового задания, когда он назначен в `jobs.engineer_ids` или `jobs.assigned_engineer`; проверки менеджера, команды задания и команды выезда сохранены.
- Production smoke в транзакции под выбранным назначенным инженером увидел одно базовое задание и одну его canonical item. Проверочная транзакция откатилась, бизнес-данные не изменены. Локальная регрессия дополнительно проверяет, что другой инженер не видит это задание.
- Проверки: lint, `npm test` 409/409, `npm run test:tz` 409/409, smoke build/preview и `git diff --check`; PR-CI повторил все проверки успешно.

## Контрольная точка: блокировка обычного редактирования импортированных строк (2026-09-24 11:52 UTC)

- PR #90 `[deploy] Lock imported request finance rows` слит squash-коммитом `c9ace4396846758a6aa5b278ca7ebdfa7a86106c`. CI run #366 и Pages deploy успешны; сайт отдаёт `assets/index-DoREIaWd.js` с метками `историческая · защищена` и `Требуется отдельная аудированная коррекция`.
- Редактор больше не предлагает менять/удалять импортированные work/material строки, подтверждать или отклонять защищённые материалы. Bulk approval работает только по обычным редактируемым строкам; legacy источник и dual-write продолжают работать.
- Локально: lint, точечные projection/request-save tests 7/7, smoke build/preview, `git diff --check`; PR-CI полностью зелёный.

## Контрольная точка: запись новых финансовых строк заявки в canonical task items (2026-09-24 12:27 UTC)

- PR #91 `[deploy] Save request finance to canonical task items` слит squash-коммитом `cf7301cf94984cbf8ddf042dac49ffa3848b85fe`. CI run #369 зелёный: lint, оба набора тестов, build и smoke; Pages deploy завершился успешно.
- Production migration `request_finance_canonical_write` применена к `anqfbljgfimoaziztdxe`; ledger version `20260924122723`. Новый клиент использует `job_request_save_canonical`, который атомарно сохраняет шапку, новые работы и материалы в базовый `service_order_items`. Менеджерское подтверждение идёт через `job_request_finance_approve`. Оба публичных wrapper RPC — `SECURITY INVOKER`, `search_path=''`; внутренние функции — в `dlight_private` с явной проверкой роли/назначения. Прямые INSERT права на `service_order_items` не выдавались; `anon` не имеет EXECUTE.
- Менеджерские тарифные снимки и revenue override сохраняются; инженерская цена/стоимость не принимается от клиента. Стабильные UUID обеспечивают повторяемый offline replay. Новые queue records помечаются canonical generation; старые записи по-прежнему идут через `job_request_save`. Legacy таблицы и dual-write triggers не удалялись.
- Защищённые импортированные строки остаются неизменяемыми. Новые строки заявки видны в проекциях, а экран задания не позволяет менять их как обычный план; триггер блокирует изменение и удаление вне редактора заявки.
- Production RLS smoke под назначенным инженером успешно вызвал canonical RPC внутри транзакции; транзакция откатилась, контрольная строка не сохранилась. Advisors не показывают новых findings по добавленным функциям; существующие замечания относятся к старым views/functions.
- Pages отдаёт `assets/index-E7iggk0w.js`; подтверждены `job_request_save_canonical`, `job_request_finance_approve`, версия очереди и метка защиты строки заявки.
- Проверки: локально lint, `npm test` 414/414, `npm run test:tz` 414/414, `npm run smoke`, `git diff --check`; CI повторил полный набор успешно.

## Контрольная точка: каталожные работы и атомарная запись заявки (2026-09-24 10:02 UTC)

- PR #83 слит (`9a59b99b10c1ef462cff63ac58281728041a79f5`), PR #84 `[deploy] Snapshot task work tariffs and costs` слит (`c51a073dc09c9840083f60bd2f6c956e988f7658`), PR #85 `[deploy] Add task work catalog and warranty fields` слит (`ccde0ee4b59fb112eeb41642d1ae02ec17f320dc`). PR #85 CI/deploy run #35971464219 успешен; production migration ledger содержит `20260924074648 task_work_catalog_and_warranty`. Pages bundle `index-Y2RrPJto.js` содержал выбор каталога, нормо-часы, платный/гарантийный режим, причину гарантии и профиль тарифа.
- Production после #85: 17 `job_works`, 23 `job_parts`, 0 task-only work и 0 task-only material. Менеджерский transactional smoke проверил каталог и серверные тариф/себестоимость; транзакция откатилась, исходные строки не изменились.
- PR #86 `[deploy] Save request headers and work lines atomically` слит squash-коммитом `bd090db8408c62ed1f131cd8630a18cfb7e6a9b8`; PR CI run #355 завершился успешно (lint, test, test:tz, build и smoke). Production migration `request_write_rpc` применена; ledger version `20260924095729`. RPC `public.job_request_save(uuid,jsonb,jsonb)` — `SECURITY INVOKER`, `search_path=''`, execute разрешён `authenticated`, запрещён `anon` и `public`.
- Обычное сохранение шапки и полного набора работ и replay полной офлайн-записи используют один RPC в одной транзакции. Работы пока пишутся в legacy `job_works`, действующие триггеры синхронно зеркалят их в `service_order_items`. Старые queue payloads без стабильного полного списка сохраняют только шапку; неизвестный стабильный ID отклоняется без вставки дубликата. `job_parts` и их очередь ещё используют прежние прямые запросы. Это промежуточная атомарность, не полное переключение на canonical source of truth. Legacy paths не выключать; семь shadow-задач не менять.
- Live transactional smoke под менеджерской RLS-ролью создал заявку и работу, сервер применил тариф 1,500 и себестоимость 750; откат выполнен. Отдельный smoke под назначенной engineer-ролью обновил шапку и добавил работу без менеджерского подтверждения; server approval остался pending, canonical строка сохранила provenance ID; откат выполнен. Счётчики после smoke: 17 заявок, 17 `job_works`, 23 `job_parts`, 40 canonical items, 0 task-only work/material.
- После merge Pages отдаёт `assets/index-nTjU7Lgf.js`; его build stamp `c2c9bad` и все 26 component hashes совпали с локальной сборкой. В bundle присутствуют `job_request_save`, каталог работ и причина гарантии. В браузере проверена публичная форма входа; взаимодействие с данными приложения не выполнялось.
- Локальные проверки: lint, `npm test` 405/405, `npm run test:tz` 405/405, `npm run build` (offline preview smoke), `git diff --check`; после финального тестового расширения точечные DB+helper тесты 24/24. Следующая порция — атомарное сохранение материалов заявки и совместимый replay частей; далее запланированы переход редакторов на canonical writes с поддержкой старой очереди, audited void/correction и отдельный аудит семи shadow-задач.


## Контрольная точка: сводка экономики работ задания (2026-09-24 06:25 UTC)

- PR #81 слит (`17bdc3f9f5225c7206ba917d259920590ebfec92`): план материалов исключает `transferred_qty`, CI #346 прошёл, Pages отдаёт новый bundle `assets/index-Cb-pygmg.js`.
- PR #82 слит (`d0a75290f6122ebc7675cd35f5f6db7856860027`), migration `carry_task_financial_snapshots` применена к production project `anqfbljgfimoaziztdxe`, ledger version `20260924051759`. Перенос теперь сохраняет атрибуты work rows и пропорциональные финансовые снимки; исходные migration provenance/approvals остаются только у исходных строк. ACL проверен: anon не вызывает private carry, authenticated вызывает через RPC. Production totals после migration не изменились (23 материала, 90 шт., 126,290 выручки / 88,451.49 себестоимости; 17 работ, 131 ч, 98,250 / 98,250).
- Текущая локальная ветка `feat/task-work-economics-summary` основана на локальных эквивалентах merged PR #81/#82. Добавляется UI сводка работ по сохранённым финансовым снимкам и план/факт; план вычитает `transferred_qty`, для cancelled задания включает только выполненное, факт строится по `done_qty`. Непрайсованные строки исключаются из сумм и помечаются в самой строке и агрегате. Материалы используют ту же cancelled-семантику.
- Регрессии покрывают перенос остатка (сумма исходной и новой строки без двойного счёта), cancelled, null snapshots и явные нулевые snapshots. На текущем diff: `npm run lint`, `npm test` (396/396), `npm run test:tz` (396/396), `npm run build`, `npm run smoke`, `git diff --check` прошли. После последней правки точечной пометки строки ещё раз выполнить таргетный тест, затем подготовить `[deploy]` PR, дождаться CI, слить и подтвердить Pages bundle.
- Более ранняя запись ниже, утверждающая, что migration не опубликована/не применена, устарела: #82 merged и production migration применена, как зафиксировано выше.
- Следующий крупный шаг: task-only tariff/cost snapshots в canonical editor/RPC; далее перевод редакторов заявки и offline replay на canonical writes с сохранением старой очереди, audited void/correction, engineer RLS live-check и безопасное retirement семи legacy shadow-задач. Не удалять legacy source paths или shadow rows автоматически.

## Архивная контрольная точка после `b046538` (2026-09-24)

## Активная работа после `b046538` (2026-09-24)

- PR #81 `[deploy] fix: avoid double counting carried task materials` слит commit `17bdc3f9f5225c7206ba917d259920590ebfec92`; main push workflow #346 завершился успешно. Live Pages после merge отдаёт свежий bundle `assets/index-Cb-pygmg.js` вместо старого `index-BetlPxAy.js`.
- Исправлена сводка стоимости материалов в задании: сумма плана вычитает `transferred_qty`, поэтому перенесённый объём считается в новом задании один раз, а факт (`done_qty`) остаётся отдельной суммой.
- Начат следующий этап в локальной ветке `feat/task-finance-snapshots`: обнаружено, что функция переноса копирует складские снимки материалов, но теряет финансовые снимки и свойства работ. Добавлена новая миграция `20260924160000_carry_task_financial_snapshots.sql`: копирует каталог/гарантийность/тарифные атрибуты работ и пропорциональную долю сохранённых доходов/себестоимости; для материалов вычисляет сумму перенесённого объёма по замороженным ценам. Исходные provenance IDs и approval метки намеренно не копируются, история исходного задания остаётся.
- Добавлен тест на перенос остатка после частичного выполнения. Полная локальная проверка после миграции: lint прошёл, `npm test` 392/392, `npm run test:tz` 392/392, build и smoke прошли; миграция пока не опубликована/не применена к production.
- Production read-only audit: `service_order_items` и `service_orders` сейчас имеют только SELECT policies; legacy `job_works` допускает менеджерские записи и ограниченные записи назначенного инженера, `job_parts` также поддерживает незавершённые авторские записи инженера. Существующие money guards сервером применяют тарифы/сбрасывают цену инженера; будущий canonical RPC обязан сохранить эти ролевые различия и тарифную логику.
- Редакторы заявки/offline replay ещё пишут `job_works/job_parts`; триггеры зеркалят их в `service_order_items`. Не отключать legacy write paths до атомарного cutover и проверки совместимости очереди. Следующий крупный этап — task-only tariff/cost snapshots в canonical editor/RPC, затем перевод обоих редакторов и offline replay, audited void/correction, engineer RLS live-check и разрешение судьбы семи legacy shadow-задач.

## Самая свежая точка: canonical financial reads (2026-09-24)

- PR #77 слит: `d2fdc65c6ed0a35b1e9999a5f2e1db24dd90960b`, merge title включал `[deploy]`. Финальный PR CI run #336 успешен (lint, весь test suite, timezone suite, build и smoke). Первоначальный run #335 поймал синтаксическую ошибку вложенного embed; скобка исправлена, #336 прошёл. **Live Pages deploy теперь подтверждён**: stamp `2e7b0cb` и все 26 хешей/счётчиков строк совпали с локальной сборкой `main` (2026-09-24 04:35 UTC). Bundle содержит canonical `service_order_items`, `trip_service_orders` и адаптер `legacy_snapshot`.
- PR #77 переводит финансовые чтения списка заявок, dashboard, ленты/карты, маршрутов/выездов, карточки заявки, загрузки материалов и очереди подтверждения на `jobs → service_orders (service_orders_job_id_fkey) → service_order_items`. Чтение `job_works/job_parts` через embed или прямой `.select` удалено из этих app queries; старый UI/offline queue пока записывает в legacy source, trigger-ы синхронизируют source → canonical.
- `src/core/legacy-finance.js` проецирует только строки с legacy provenance и восстанавливает поля старого API из `legacy_snapshot` и текущих canonical snapshot columns. Task-only scope пока не попадает в старые фин. агрегаты: правила цены/переноса/частичного факта ещё не реализованы; нельзя молча посчитать строку дважды. Это переходный мост, не завершённая модель экономики.
- Удаление/отклонение импортированных работ и материалов заблокировано в старой форме; source delete trigger блокирует его и в БД. Разрешать удаление можно после разработки audited void/correction процесса.
- Production migration ledger: `20260923230626 legacy_finance_to_task_items`, `20260923230641 legacy_finance_dualwrite`. Сверка: 17/17 работ, 131 час, 98,250 выручки, 98,250 себестоимости; 23/23 материала, 90 шт., 126,290 выручки, 88,451.49 себестоимости. Все source snapshots совпали построчно, `done_qty=0` у импортированных строк, 17 записей migration history. Источники сохранены.
- PR #76 со значительно более подробной предыдущей контрольной точкой слит (`88f6e465e47cad65606e1248111f34b2c953cfa1`); этот раздел новее и дополняет его.

### Контрольная точка после восстановления workspace (2026-09-24 05:36 UTC)

- Локальный клон восстановлен с `origin/main` на `c24bad41828b15b5513bdb5e57987ab0e5a03b3e`; рабочее дерево чистое. `npm ci`, `npm run lint`, `npm test` (391/391), `npm run test:tz` (391/391), `npm run build` и `npm run smoke` прошли. Первый запуск двух полных Vitest-прогонов одновременно уронил worker-процессы из-за конкуренции; отдельные повторные прогоны прошли полностью.
- Production Postgres inventory для старых trip-shadow заданий: 7 строк с `job_id IS NULL` и `legacy_trip_id`; все семь ссылаются на существующий выезд и имеют по одной записи миграционной истории (у задания №5 — две). У каждой сохранены job snapshots: пять заданий охватывают одну заявку, два — по две. Прямых `trip_service_orders` от shadow ID нет; выезды связаны с заданием каждой обслуживаемой заявки по canonical M:N таблице. Комментариев на shadow заданиях нет. Эти строки исключены из task kanban по `job_id IS NOT NULL`; не удалять и не объединять автоматически: `trips.service_order_id` и `legacy_trip_id` остаются переходными ссылками, пока не определено безопасное retirement/compatibility поведение.
- PostgREST embed и RLS под аутентифицированной production-ролью отдельно ещё не проверены; совпадение build stamp доказывает deployed code, но не успешность runtime-запроса в живой сессии.

### Следующие действия

1. Проверить PostgREST embeds `jobs → service_orders → service_order_items` и RLS в production через аутентифицированный живой сеанс; CI и stamp этого не доказывают.
2. Описать экономику task-only work/material rows: work pricing/tariff, подтверждение, план/факт, billable revenue, частичное выполнение, `transferred_qty`/carry и правило суммирования без дубля. Заменить переходный adapter полноценной canonical economics model.
3. Перевести request editor writes/offline queue на canonical item RPC/триггеры. Сейчас только legacy → canonical dual-write; обратная совместимость старых клиентов нужна до отключения legacy endpoints.
4. Разработать audited void/correction для work/parts; не снимать DELETE guard раньше. Проверить approve/reject, engineer edits, offline replay и RLS.
5. На основе уже выполненной инвентаризации семи shadow-заданий определить совместимое retirement-поведение для `trips.service_order_id`/`legacy_trip_id`; сохранить историю, не объединять/удалять автоматически.
6. Продолжать регулярно обновлять checkpoint. Пользователь разрешил автономные действия в репозитории и Supabase; не запрашивать разрешение повторно.

---

## Контрольная точка: финансовый backfill и продолжение cutover (2026-09-24)

Это верхнее состояние актуально; нижние пункты с более ранней датой описывают историю. Пользователь разрешил автономную работу в репозитории и production Supabase и поручил завершить переход на «заявка → задание ↔ выезд», включая связи, историю/таймлайн, комментарии, финансовые данные и миграцию. Общедоступный старый чат напрямую не удалось открыть; цель и согласованные инварианты восстановлены из предыдущей контрольной точки и репозитория.

### Что завершено в этой порции

- Live GitHub Pages подтверждён по bundle stamp `ca4eef3`; это код PR #74 со структурой заданий/карточек. PR #75 «feat: sync legacy finance into request tasks» слит в `b8f430377a8977cfb4794df041e99a9bffc9d669`. CI run #331 прошёл lint, unit/PGlite tests, timezone tests, build и smoke; deploy пропущен (в PR только DB-триггеры/тесты).
- Production до backfill: 17 строк `job_works` (131 ч, выручка 98,250, себестоимость 98,250 при 750/ч), 23 строки `job_parts` (90 шт., выручка 126,290, себестоимость 88,451.49), нулевые/отрицательные количества отсутствовали, у всех parts уже была позиция каталога.
- Backfill применён через Supabase connector; журнал проекта записал версии `20260923230626 legacy_finance_to_task_items` и `20260923230641 legacy_finance_dualwrite` (версии журнала — источник истины; имена SQL-файлов начинаются с `20260924...`).
- Production после миграций: все 17 работ и 23 материала связаны с `service_order_items`; каждый из 40 `legacy_snapshot` совпадает с исходной строкой; часы/количества и выручка/себестоимость совпали с baseline; `done_qty=0` для всех перенесённых строк; 0 строк с некорректным provenance/родительским заданием; создано 17 базовых заданий и 17 записей истории миграции. Старые источники сохранены.
- Trigger dual-write синхронизирует старые write paths с canonical task items; удаление уже перенесённой legacy-строки и смена её заявки блокируются до определения поддержанного процесса аннулирования. Миграция после установки триггеров повторно reconciles строки, чтобы закрыть окно между backfill и установкой синхронизации.
- Таймлайн и отдельные комментарии заявки/задания/выезда уже есть в опубликованном UI и production schema. После миграций таблицы комментариев пока пусты.

### Незавершённая работа и срочная точка

В локальной рабочей копии начат, но **не опубликован и не слит** следующий read cutover: `src/core/legacy-finance.js` проецирует только строки с legacy provenance из canonical task items в старую форму данных; списочные/маршрутные query в `src/app.js` и запрос `src/service-orders.js` уже переключены с nested `job_works/job_parts` на `service_orders!service_order_id_fkey(service_order_items(*))`. Projection пока намеренно фильтрует новые task-only строки, чтобы не подменить финансовую методику и не удвоить переносимый остаток; это временный мост, не финальная экономика. После этих локальных правок прошли lint, `npm test` (391), `npm run build`; timezone/smoke для этой новой порции и PostgREST live-query ещё не повторены.

Перед публикацией исправь UX удаления: старая форма заявки до сих пор показывает «удалить/отклонить» для перенесённых финансовых строк, но production guard уже блокирует delete. Пометь projection-строки task item ID, скрой/заблокируй удаление/отклонение и поставь backend/UI guard до удаления; не удаляй исторические строки. То же для `job_works`: защити diff-delete в `persistJobWorks`.

Затем:

1. Восстанови подключение к workspace и сначала сверь `git status`; локальная ветка основана на старом SHA и содержит исторически uncommitted изменения PR #72–74, уже эквивалентные `main`. Делай новую PR-ветку строго от актуального remote `main`; включай только проверенный diff cutover, UX-guard, нужные тесты и обновлённые документы. Перед upload сравни remote blob content с локальными байтами; GitHub connector раньше усекает большие содержимые при прямой передаче.
2. Проверь PostgREST embed `jobs → service_orders (по service_orders_job_id_fkey) → service_order_items` на production/read-only. Классифицируй оставшиеся упоминания старых таблиц: source writes/offline queue могут оставаться временно, но все финансовые reads карточек/списков/маршрутов/economics/approval queue должны читать canonical projections.
3. Запусти lint, весь test suite, `test:tz`, build, smoke; дождись CI для конечного commit. PR read cutover публикуй через deploy title, затем подтверди новый Pages stamp и UI с доступной ролью.
4. Следующая порция — перевести editor writes на canonical task items с односторонней совместимостью старых клиентов/очередей. Сейчас поток только legacy→canonical; обратного canonical→legacy потока нет. Не отключай source пишет/не удаляй таблицы до полного аудита старых клиентов/offline snapshots.
5. В проектной экономике отдельно определить, как учитывать новые task-only work/material lines, transfers/carry, частичный факт и billable revenue, чтобы старые отчёты не исключали реальные строки и не считали один перенесённый объём дважды. Не приравнивай финансовое подтверждение к выполненному `done_qty`.
6. Отдельно инвентаризируй семь старых task placeholders с `job_id=NULL` и `legacy_trip_id`; связывай их с каноническими заданиями только с сохранением истории/старых ссылок, не удаляй.
7. Обновляй эту контрольную точку после каждого проверенного этапа.

Обновлено: 2026-09-24. Репозиторий: `DlightKND/route-planner`; Supabase: `anqfbljgfimoaziztdxe`; действующий сайт: https://dlightknd.github.io/route-planner/. Язык общения с пользователем — русский. Пользователь разрешил продолжить работу, включая PR, слияние, совместимую миграцию и публикацию. Перед каждой внешней записью проверь соединение, проект и исходные значения; не выводи секреты.

## Продолжение работы

Переход к модели **заявка → задание ↔ выезд** начат, но не завершён. PR #62, #63, #65, #66, #67 и #68 слиты; PR #68 исправил неоднозначный embed заявки. Текущая авторизованная сессия сайта ранее подтверждена как `admin`. Физический склад не запускается без согласованных мест хранения и начальной инвентаризации.

Последний аудит нашёл три регрессии на границах: перенос остатка терял прямой `job_id`; обновление старой `trip_jobs.job_id` оставляло прежние задания в выезде; замена задания могла стереть явное распределение подтверждённой стоянки и оставить ссылку на отключённое задание. Исправление выпущено в PR #69 (merge commit `fa3e85b`) и применено к production миграцией `20260923205517_restore_task_link_invariants` (имя версии в журнале Supabase). Run #311 завершился успешно. До и после миграции совпали контрольные данные: 17 заявок, 15 заданий, 7 выездов, 9 связей `trip_jobs`, 9 `trip_service_orders`, 49 стоянок, 18 долей, 0 строк материалов и 0 запусков расчёта затрат; отсутствующих связей или стоянок/долей на чужих выездах нет. Подтверждённые доли: 16 стоянок, 23.1667 чел.-ч; сумма долей — 18. Сохранены также финансовые контрольные суммы: пробег 4100.3 км, снимки себестоимости выездов 179643.4375, `job_parts` — 23 строки / 90 шт. / 88451.49.

Новая контрольная точка, 2026-09-23: PR #71 слит (`f205117edc27a84c8f3bb332fbcda5f64bf4540a`); production migration ledger содержит `20260923213431 entity_activity_comments`. Добавлены отдельные таблицы комментариев к заявке/заданию/выезду, аудит изменений заявок и RLS-видимость истории выезда для назначенной команды. GitHub Actions PR run #315 прошёл; после слияния сайт Pages в сессии `admin` показал отпечаток `1cfb67e`, совпадающий с локальной сборкой из PR #71. Новые комментарии/события заявок пока 0; финансовые источники не менялись. `npm run lint`, `npm test` (374), `npm run test:tz` (374), `npm run smoke` и `git diff --check` прошли перед PR.

Полный переход всё ещё не завершён. Production snapshot: 17 заявок; 15 заданий (8 канонических и 7 старых пустых записей по выездам); 7 активных выездов; 9 связей `trip_service_orders`; 0 строк `service_order_items`; 17 подтверждённых работ и 23 подтверждённых материала в старых таблицах. Сверка денег: работы 131 ч / 98,250 выручки; материалы 90 шт. / 126,290 продажи / 88,451.49 закупочной себестоимости. Оба старых источника активно используются интерфейсом и расчётами.

PR #72 `fix: preserve stable IDs for job work rows` слит 2026-09-24 (`9eca826bf01d4567db8a0677e0216c35bd621f72`), CI run #321 прошёл lint, 380 тестов, timezone-тесты, build и smoke. Он исправил пересоздание строк `job_works`, сохранил provenance ID и защитил старые offline snapshots. После merge запрошен deploy через `[deploy]`; live stamp ещё не подтверждён.

Локально подготовлена миграция `20260924091500_legacy_finance_to_task_items.sql`: аддитивные поля происхождения/подтверждения/денег, создание черновых seed-заданий для заявок с legacy финансовыми строками и перенос каждой работы/детали с полным снимком и точной финансовой суммой. `done_qty` не выводится из старого учёта и остаётся 0. Источники не удаляются. Миграция ещё не применена к production. PGlite-тест миграции прошёл (14 тестов); требуется повторный полный набор и ревью SQL/CI перед merge и применением. Далее обязательно переключить все чтения и записи на канонические строки и совместимый переход; не считать backfill финальным переключением.

Проверка качества исправлений: в PR #69 добавлены регрессионные тесты трёх инвариантов; локально прошли lint, 370 тестов, те же 370 под `America/Los_Angeles`, build и smoke. Обнаруженная при аудите лишняя миграция `20260923200000_service_order_ownership.sql` была удалена из локального черновика: она предполагала ещё не согласованные полномочия владельца/куратора, не попала в PR #69 и никогда не применялась к production. Не восстанавливай её без отдельного согласования модели ролей.

Перед правками проверь `git status`, актуальную `main`, production-схему и историю миграций. Основной проектный документ — `docs/service-orders-design.md`. Пользователь разрешил коммиты, PR, слияние, продуктивные миграции и публикацию; проверь наличие соединений и не выводи секреты.

## Что уже согласовано с пользователем

1. **Три главные сущности**: заявка `jobs` описывает клиента, потребность, технику, место и SLA; задание `service_orders` описывает исполнение одной заявки; выезд `trips` хранит исключительно выездные данные (маршрут, трек, стоянки, пробег, поездочные расходы). У одной заявки может быть несколько заданий. Одно задание относится ровно к одной заявке. Один выезд может объединять несколько заданий, в том числе из разных заявок; одно задание может участвовать в нескольких выездах.
2. Канбан заявок сохраняется. Дочерние задания доступны из заявки, задания имеют диспетчерский обзор/канбан; общий выезд показывается по одному ID в каждом связанном задании.
3. **Разовый исторический перенос:** для каждой уже существующей заявки с выездом создать **одно базовое задание на `job_id`**, а не задание на каждый выезд. Все её старые выезды привязать к этому заданию. Общий выезд привязать к базовому заданию каждой включённой заявки. Старые подтверждённые стоянки с `job_id` разрешено отнести к базовому заданию той же заявки, сохранив исходные ID, время, людей, подтверждение и часы. Это разовое заполнение, не правило для вновь создаваемых заданий. Повторный запуск переноса не создаёт дублей.
4. У каждой сущности собственный статус и история. Целевые стадии: черновик → запланировано → в работе → на проверке → закрыто; отменено — отдельный исход. Изменение дочернего статуса не меняет автоматически родительский. Показатель прогресса дочерних сущностей вычисляется отдельно.
5. Владелец, куратор и исполнители; исполнитель предлагает переносы/правки, куратор утверждает. Нужна оргструктура сотрудников и руководителей для контролируемой передачи владения. Задание хранит построчные материалы со снимком цены и себестоимости; во вкладке каталога нужен «Склад». Не вводи фиктивные остатки склада из старых затрат.
6. Выручка и себестоимость — разные распределения. Распределение транспортной выручки по плательщикам уже есть. Для транспортной себестоимости согласовано направление: фактический трек разбивается на участки по остановкам, затрата относится к заданиям соответствующей точки; нераспределённые участки остаются видимыми. Конкретные исключения и суточные/ночлег требуют отдельной реализации и проверки.

## Текущее состояние после PR #63

- PR #62 создал каноническое задание на одну заявку и связь `trip_service_orders` многие-ко-многим. `trip_jobs`, `trips.service_order_id` и переходные триггеры сохранены для совместимости.
- PR #63 добавил `trip_stay_task_allocations`, явный флаг долей, атомарное сохранение, manager-only проверки, `trip_cost_allocation_runs` и `trip_cost_allocations`. Миграция применена к `anqfbljgfimoaziztdxe`: сохранены 18 исторических долей по 100%, невалидных ссылок нет, ни один финансовый расчёт автоматически не подтверждался. Последняя повторная сверка до исправления также показала 0 текущих расчётов затрат.
- Подтверждённый труд распределяется по человеко-часам и долям стоянок; себестоимость пробега — по надёжным сегментам трека и подтверждённым остановкам. Недостающая геометрия, остаток одометра, суточные, ночлег и ручные корректировки остаются видимыми нераспределёнными суммами. Распределение выручки не менялось.
- Расчёты привязаны к ревизии выезда и трека; устаревшие итоги скрываются в сводках заданий/заявок. PR CI и deploy workflow прошли. Авторизация в действующем сайте подтверждена видимым состоянием приложения, роль `admin`; каталог и склад проверены в production. Загруженный bundle содержит `trip_presence_save_tasks`, `trip_cost_allocation_save` и `service_order_trip_cost_summary`.
- Роли: `admin`, `logist`, `engineer` на базе `profiles.id`; роли `moderator` нет. `work_catalog.materials` — JSON-предложения, `job_parts` — исторические финансовые строки; полноценной номенклатуры и физического склада пока нет.

## Следующие этапы

### 1. Базовая оргструктура: готова

PR #65 смержен, migration `20260923153219 employee_org_structure` применена, deploy workflow завершился успешно. На последней проверке таблица содержала 3 строки на 3 профиля, 0 сирот; RLS включён. Настройки управляют должностью и прямым руководителем через admin-only RPC; циклы, самоназначение и неактивный руководитель запрещены. Текущая сессия сайта подтверждена под `admin`. Это ещё не назначения владельцев, кураторов или автоматическая передача.

### 2. Расширить оргструктуру до назначений и владения

Проведи read-only аудит `profiles`, текущих ролей, команд и назначений. Проектируй иерархию поверх `profiles.id`, не меняя идентификаторы и RLS без необходимости. До миграции зафиксируй правила активного сотрудника, руководителя, подчинённых, владельца и куратора. Проверь циклы в иерархии, отключённого или неуполномоченного руководителя и передачу владения при отсутствии подходящего руководителя. Предложение исполнителя о переносе срока/правке должно ждать решения куратора.

### 2. Каталог и материалы заданий

Миграция `20260923154823 stock_catalog` применена, PR #66 смержен и опубликован; 23/23 старых `job_parts` перенесены по одному через `legacy_part_id`, 0 сирот, исходные финансовые строки сохранены. Вкладка «Склад» проверена после входа на production-сайт. Физических остатков нет: начальная инвентаризация не проводилась.

PR #67 смержен и опубликован; он добавил тип `material` к `service_order_items`, выбор активной позиции из каталога, серверный снимок имени/SKU/единицы/цены/себестоимости и сводку план/факт по снимку. Миграция `20260923184850` применена; на предыдущей проверке таблица была пуста: 0 строк, 15 заданий, 23 позиции каталога. Повторное сохранение не подтягивает новую цену; перенос остатка должен сохранять исходный снимок и теперь покрыт новой регрессией. PR #68 задаёт `jobs!service_orders_job_id_fkey`, добавляет UI-тест и уже слит/опубликован. Рецептуры `work_catalog.materials` и подтверждаемое обновление карточки цены остаются будущими задачами. Перед физическими остатками согласовать места хранения, резервы, движения и инвентаризацию.

### 3. Оставшиеся правила поездочной экономики

Труд и пробег уже распределяются на задания. Для суточных, ночлега и ручных корректировок до реализации нужна согласованная база. Сохраняй равенство «назначено + не распределено = подтверждённая сумма» и не меняй распределение маршрутной выручки по плательщикам. Проверяй петли и повторы трека, пропуски GPS, обратный сегмент и несовпадение GPS с одометром.

## Проверки и выпуск

- Дай интеграционные тесты миграции: одна заявка с 2+ выездами → одно базовое задание; выезд с 2+ заявками → столько связей с базовыми заданиями, сколько уникальных заявок; повторная миграция → те же IDs и количество; стоянка с `job_id` → ровно одна историческая запись и правильное задание; без `job_id` → исключение, а не фальшивая связь. Отдельно проверь существующие ручные задания, старые строки с `done_qty` и `source_item_id`, мягко удалённые выезды и RLS инженера/диспетчера.
- Проверяй полный пользовательский сценарий на предпросмотре (десктоп и узкий экран), особенно канбан, карточку задания, общий выезд и стоянки. Запусти `npm run lint`, `npm test`, `npm run test:tz`, `npm run build` и `node tests/smoke.cjs dist/index.html`. В CI `.github/workflows/deploy.yml` повторяет эти проверки. PGlite-тесты не заменяют проверку фактической схемы и RLS на Supabase.
- Перед продуктивной миграцией зафиксируй исходные агрегаты и зависимости, проверь историю версии на конкретном проекте `anqfbljgfimoaziztdxe`. Применяй только совместимую, проверенную миграцию; после неё проверь число связей, часов, итогов и работу старых экранов, затем переключай сайт. Если миграция или проверка не прошла, не запускай публикацию несовместимой сборки.
- GitHub Pages собирает `dist` из `main`. PR и обычный push выполняют сборку без публикации; деплой происходит после успешной сборки по `workflow_dispatch` на `main` либо при `[deploy]` в сообщении push-коммита. После слияния и публикации проверь успешный workflow, загрузку действующего сайта, штамп сборки из `vite.config.js` и основные сценарии с доступной ролью. Разрешение на PR/merge/deploy есть, повторно спрашивать его не нужно.

## Что сообщить пользователю

Кратко: что реально реализовано, номера PR/коммита и версии применённых миграций, что показала сверка до/после, адрес и состояние действующего сайта, какие исключения остались. Не говори, что новая модель заработала, если переведены лишь схема или документ. Каждый этап выпускай отдельной проверяемой порцией. После изменения доступа проверяй RLS; после правок учёта сверяй подтверждённые факты и агрегаты; после UI-публикации проверь действующий bundle и доступные роли.
