{
  "name": "dlight",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "smoke": "npm run build && node tests/smoke.cjs dist/index.html",
    "lint": "eslint src"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0",
    "eslint": "^9.0.0"
  }
}
