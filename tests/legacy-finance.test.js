import {describe,it,expect} from 'vitest';import {legacyFinanceForTasks,projectLegacyFinance} from '../src/core/legacy-finance.js';describe('canonical task finance projection',()=>{it('projects source snapshots into request accounting fields',()=>{const j=projectLegacyFinance({id:'j',service_orders:[{service_order_items:[{id:'wi',legacy_job_work_id:'w',work_catalog_id:'cat',title:'Work',planned_qty:2,billable:false,billable_reason:'Warranty',financial_revenue_snapshot:700,approved_by:'mgr',legacy_snapshot:{materials:[1],revenue_override:null}},{id:'pi',legacy_job_part_id:'p',title:'Part',unit:'шт',planned_qty:3,sku_snapshot:'S',unit_price_snapshot:120,unit_cost_snapshot:75,legacy_snapshot:{created_by:'user'}}]}]});expect(j.job_works[0]).toMatchObject({id:'w',legacy_task_item_id:'wi',hours:2,revenue:700,billable:false,approved_by:'mgr'});expect(j.job_works[0].materials).toEqual([1]);expect(j.job_parts[0]).toMatchObject({id:'p',legacy_task_item_id:'pi',name:'Part',qty:3,price:120,cost:75,created_by:'user'});});it('ignores task-only lines and duplicate source provenance',()=>{const x=legacyFinanceForTasks([{service_order_items:[{id:'1',legacy_job_work_id:'w',planned_qty:2},{id:'2',legacy_job_work_id:'w',planned_qty:9},{id:'3',kind:'work',planned_qty:4}]}]);expect(x.job_works).toHaveLength(1);expect(x.job_works[0].hours).toBe(2);expect(x.job_parts).toEqual([]);expect(projectLegacyFinance({id:'thin'})).toEqual({id:'thin'});});});